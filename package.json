{
  "name": "ulong-tech-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf public && mkdir -p public && cp -R index.html styles.css script.js assets robots.txt sitemap.xml public/",
    "dev": "python3 -m http.server 4173"
  }
}
